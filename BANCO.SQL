CREATE DATABESA empresa;

CREATE TABLE TBLCLIENTES(
	ID SERIAL PRIMARY KEY,
	NOME VARCHAR(75) NOT NULL,
	CPF_CNPJ VARCHAR(20) NOT NULL,
	ENDERECO VARCHAR(80) NOT NULL,
	CEP VARCHAR(80) NOT NULL,
	TELEFONE VARCHAR(15) 
);

CREATE TABLE TBLPRODUTOS(
	ID SERIAL PRIMARY KEY,
	DESCRICAO VARCHAR(80),
	CUSTO_REAL FLOAT,
	PRECO_VENDA FLOAT, 
	QTD_ESTOQUE FLOAT,
	UNIDADE VARCHAR(5)
);

CREATE TABLE TBLUSUARIO(
	ID SERIAL PRIMARY KEY, 
	NOME VARCHAR(50),
	USUARIO VARCHAR(50),
	SENHA VARCHAR(40),
	ATIVO INT DEFAULT 1,
	NIVEL INT
);

INSERT INTO TBLUSUARIO(NOME, USUARIO, SENHA, NIVEL)
VALUES('HESLI HENDRIK AZEVEDO', 'HESLI', '123', 3);
INSERT INTO TBLUSUARIO(NOME, USUARIO, SENHA, NIVEL)
VALUES('TESTE 1', 'HESLI1', '1234', 2);
INSERT INTO TBLUSUARIO(NOME, USUARIO, SENHA, NIVEL)
VALUES('TESTE 2', 'HESLI2', '12345', 1);

SELECT * FROM TBLUSUARIO

DELETE FROM TBLUSUARIO WHERE ID BETWEEN 1 AND 3